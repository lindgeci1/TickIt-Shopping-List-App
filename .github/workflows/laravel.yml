name: Laravel CI (PostgreSQL)

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  laravel-tests:
    runs-on: ubuntu-latest

    services:
      postgres:
        image: postgres:15
        env:
          POSTGRES_DB: laravel_test
          POSTGRES_USER: postgres
          POSTGRES_PASSWORD: ${{ secrets.DB_PASSWORD }}
        ports:
          - 5432:5432
        options: >-
          --health-cmd="pg_isready -U postgres"
          --health-interval=10s
          --health-timeout=5s
          --health-retries=5

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up PHP
        uses: shivammathur/setup-php@v2
        with:
          php-version: '8.3'
          extensions: mbstring, bcmath, intl, pdo_pgsql

      - name: Copy environment file
        run: cp backend/.env.example backend/.env

      - name: Update environment for CI
        run: |
          sed -i 's/DB_CONNECTION=.*/DB_CONNECTION=pgsql/' backend/.env
          sed -i 's/DB_HOST=.*/DB_HOST=127.0.0.1/' backend/.env
          sed -i 's/DB_PORT=.*/DB_PORT=5432/' backend/.env
          sed -i 's/DB_DATABASE=.*/DB_DATABASE=laravel_test/' backend/.env
          sed -i 's/DB_USERNAME=.*/DB_USERNAME=postgres/' backend/.env
          sed -i "s/DB_PASSWORD=.*/DB_PASSWORD=${{ secrets.DB_PASSWORD }}/" backend/.env
          echo "APP_ENV=testing" >> backend/.env
          echo "APP_DEBUG=true" >> backend/.env

      - name: Install Composer dependencies
        run: composer install --no-progress --no-interaction --prefer-dist
        working-directory: backend

      - name: Generate application key
        run: php artisan key:generate
        working-directory: backend

      - name: Cache configuration
        run: php artisan config:cache
        working-directory: backend

      - name: Run migrations
        run: php artisan migrate --force
        working-directory: backend

      - name: Run tests
        run: php artisan test
        working-directory: backend
